" C 文件类型配置
setlocal expandtab           " 使用空格代替 Tab（Linux 内核风格除外）
setlocal shiftwidth=4        " 自动缩进 4 空格
setlocal softtabstop=4       " Tab 键插入 4 空格
setlocal tabstop=8           " 显示制表符宽度为 8（传统 C 风格）
setlocal cinoptions=:0,g0    " 调整 C 缩进细节（例如 case 标签对齐）
setlocal cindent             " 启用 C 风格缩进
